# 04 ML Classification

This module contains the classification stage of the project, including feature-generation scripts, baseline models, deep-learning models, and saved evaluation outputs.

## Structure

- `scripts/`: classical ML baselines, CNN models, continuous-inference code, and spectrogram generation.
- `results/`: saved experiment outputs grouped by model family or evaluation setting.
